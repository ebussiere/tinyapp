<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-primary">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav mr-auto">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        <%if (user.email) { %>
        <%}%>
      </div>
      <%if (!user.email) { %>
        <!-- <form class="form-inline" style="margin:0px;" action="/login" method="POST"> -->
        <a class="btn btn-outline-light my-0 mx-2" href="/login" role="button">Login</a>
        <a class="btn btn-outline-light my-0 mx-2" href="/register" role="button">Register</a>
        <!-- </form> -->
        <% } else {%>
        <form class="form-inline" style="margin:0px;" action="/logout" method="POST">
          <div class="nav-item nav-link text-white">Logged in as: <%= user.email %></div>
          <button type="submit" class="btn btn-outline-light my-2 my-sm-0">Logout</button>
        </form>
        <%}%>
    </div>
    </div>
  </nav>
</header>